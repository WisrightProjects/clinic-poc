DATABASE_URL=postgresql://postgres:password@localhost:5432/clinic_poc

# Speech-to-text provider: 'whisper' (free, open-source, default) or 'sarvam' (paid).
# 'whisper' requires the stt-service/ sidecar to be running (see stt-service/README.md).
STT_PROVIDER=whisper
WHISPER_URL=http://localhost:8000
STT_LANGUAGE=auto

# Only used when STT_PROVIDER=sarvam:
SARVAM_API_KEY=

# Summary generation is still stubbed for the POC:
USE_MOCK_SUMMARY=true

AUDIO_DIR=./uploads
CORS_ORIGINS=http://localhost:5173,http://localhost:8081
PORT=4000
